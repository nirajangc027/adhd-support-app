import 'package:flutter/material.dart';

class DumpScreen extends StatelessWidget {
  const DumpScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Dump')),
    );
  }
}
